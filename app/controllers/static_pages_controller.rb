class StaticPagesController < ApplicationController
  def home
    #executes the code and then renders the view
    #because there is no code, it just renders the view
  end

  def help
  end

  def about
  end
end
